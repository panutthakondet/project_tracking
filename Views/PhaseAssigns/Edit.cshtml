@model ProjectTracking.Models.PhaseAssign

@{
    ViewData["Title"] = "Edit Assignment";
}

<!-- ================= HEADER ================= -->
<div class="mb-4">
    <h2 class="fw-bold">✏️ Edit Assignment</h2>
    <p class="text-muted mb-0">
        แก้ไขการมอบหมายงานของพนักงาน
    </p>
</div>

<!-- ================= FORM CARD ================= -->
<div class="card shadow-sm">
    <div class="card-body">

        <form asp-action="Edit"
              method="post"
              onsubmit="return confirm('บันทึกการแก้ไขหรือไม่?');">

            @Html.AntiForgeryToken()

            <!-- ================= KEYS ================= -->
            <input type="hidden" asp-for="AssignId" />
            <input type="hidden" asp-for="PhaseId" />

            <div class="row g-3">

                <!-- ================= EMPLOYEE ================= -->
                <div class="col-md-6">
                    <label class="form-label fw-semibold">
                        👤 Employee
                    </label>
                    <select asp-for="EmpId"
                            asp-items="ViewBag.Employees"
                            class="form-select"
                            required>
                        <option value="">-- Select Employee --</option>
                    </select>
                    <span asp-validation-for="EmpId"
                          class="text-danger"></span>
                </div>

                <!-- ================= PHASE / ROLE (READ ONLY) ================= -->
                <div class="col-md-6">
                    <label class="form-label fw-semibold">
                        🧩 Phase (Role)
                    </label>
                    <input type="text"
                           class="form-control"
                           value="@Model.Role"
                           readonly />
                    <small class="text-muted">
                        * Role ถูกกำหนดจาก Phase ของ Project
                    </small>

                    <!-- ส่งค่า Role กลับไปให้ Controller -->
                    <input type="hidden" asp-for="Role" />
                </div>

                <!-- ================= PLAN START ================= -->
                <div class="col-md-3">
                    <label class="form-label fw-semibold">
                        📅 Plan Start
                    </label>
                    <input asp-for="PlanStart"
                           type="date"
                           class="form-control" />
                </div>

                <!-- ================= PLAN END ================= -->
                <div class="col-md-3">
                    <label class="form-label fw-semibold">
                        📅 Plan End
                    </label>
                    <input asp-for="PlanEnd"
                           type="date"
                           class="form-control" />
                </div>

                <!-- ================= ACTUAL START ================= -->
                <div class="col-md-3">
                    <label class="form-label fw-semibold">
                        ▶️ Actual Start
                    </label>
                    <input asp-for="ActualStart"
                           type="date"
                           class="form-control" />
                </div>

                <!-- ================= ACTUAL END ================= -->
                <div class="col-md-3">
                    <label class="form-label fw-semibold">
                        ⏹ Actual End
                    </label>
                    <input asp-for="ActualEnd"
                           type="date"
                           class="form-control" />
                </div>

                <!-- ================= REMARK ================= -->
                <div class="col-md-12">
                    <label class="form-label fw-semibold">
                        📝 Remark
                    </label>
                    <textarea asp-for="Remark"
                              class="form-control"
                              rows="3"
                              placeholder="รายละเอียดเพิ่มเติม / หมายเหตุ"></textarea>
                </div>

            </div>

            <!-- ================= BUTTONS ================= -->
            <div class="mt-4 d-flex gap-2">
                <button type="submit"
                        class="btn btn-primary">
                    💾 Save Changes
                </button>

                <a asp-action="Index"
                   asp-route-projectId="@ViewBag.ProjectId"
                   class="btn btn-outline-secondary">
                    ↩ Back
                </a>
            </div>

        </form>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}