@model List<ProjectTracking.Models.PhaseAssign>

@{
    ViewData["Title"] = "ปริมาณงานทีม";

    int year = ViewBag.Year;
    int yearTo = ViewBag.YearTo ?? year;
    int month = ViewBag.Month;
    int monthTo = ViewBag.MonthTo ?? month;

    var firstDay = new DateTime(year, month, 1);
    var endDay = new DateTime(yearTo, monthTo, DateTime.DaysInMonth(yearTo, monthTo));
    var totalDays = (endDay - firstDay).Days + 1;

    string selectedEmpId = Context.Request.Query["empId"].ToString();

    var employees = Model
        .Where(x => x.Employee != null)
        .GroupBy(x => new
        {
            x.EmpId,
            EmpName = x.Employee!.EmpName ?? ""
        })
        .OrderBy(x => x.Key.EmpName)
        .ToList();
}

<div class="container-fluid mt-3 px-2 px-md-3 d-flex justify-content-center workload-page-wrap">

    <div class="card shadow-sm border-0" style="max-width: 100%;">
        <div class="card-body">

            <div class="mb-3">

                <div>
                    <h4 class="mb-1">
                        📅 ปริมาณงานทีม
                    </h4>

                    <div class="d-flex align-items-center gap-3 small text-muted workload-legend">
                        <div class="d-flex align-items-center gap-1">
                            <span class="legend-dot legend-done"></span>
                            <span>DONE</span>
                        </div>

                        <div class="d-flex align-items-center gap-1">
                            <span class="legend-dot legend-progress"></span>
                            <span>IN_PROGRESS</span>
                        </div>
                    </div>
                </div>

                <div class="filter-toolbar d-flex align-items-center mt-3">
                    <form id="filterForm"
                          method="get"
                          class="d-flex align-items-center gap-2 mb-0 employee-filter-wrap"
                          style="width:auto; flex:none;">

                        <input type="hidden" name="year" value="@year" />

                        <label class="fw-bold mb-0 text-nowrap">
                            ผู้รับผิดชอบ
                        </label>

                        <div style="width:250px; min-width:250px; flex:0 0 250px;">
                            <select id="empFilter"
                                    name="empId"
                                    class="form-select emp-select-fixed"
                                    onchange="this.form.submit()">

                                <option value="">
                                    -- ทั้งหมด --
                                </option>

                                @foreach (var emp in employees)
                                {
                                    <option value="@emp.Key.EmpId"
                                            selected="@(selectedEmpId == emp.Key.EmpId.ToString())">
                                        @emp.Key.EmpName
                                    </option>
                                }

                            </select>
                        </div>

                        <label class="fw-bold mb-0 text-nowrap">
                            ปีเริ่ม
                        </label>

                        <div style="width:110px; min-width:110px; flex:0 0 110px;">
                            <select name="year"
                                    class="form-select"
                                    form="filterForm">
                            @for (int y = DateTime.Today.Year - 2; y <= DateTime.Today.Year + 3; y++)
                            {
                                <option value="@y" selected="@(year == y)">@(y + 543)</option>
                            }
                            </select>
                        </div>

                        <label class="fw-bold mb-0 text-nowrap">
                            เดือน
                        </label>

                        <div style="width:180px; min-width:180px; flex:0 0 180px;">
                            <select name="month"
                                    class="form-select month-select"
                                    form="filterForm">
                            @for (int m = 1; m <= 12; m++)
                            {
                                <option value="@m" selected="@(month == m)">
                                    @(new DateTime(year, m, 1).ToString("MMMM", new System.Globalization.CultureInfo("th-TH")))
                                </option>
                            }
                            </select>
                        </div>

                        <span>ถึง</span>

                        <label class="fw-bold mb-0 text-nowrap">
                            ปีสิ้นสุด
                        </label>

                        <div style="width:110px; min-width:110px; flex:0 0 110px;">
                            <select name="yearTo"
                                    class="form-select"
                                    form="filterForm">
                            @for (int y = DateTime.Today.Year - 2; y <= DateTime.Today.Year + 3; y++)
                            {
                                <option value="@y" selected="@(yearTo == y)">@(y + 543)</option>
                            }
                            </select>
                        </div>

                        <label class="fw-bold mb-0 text-nowrap">
                            เดือน
                        </label>

                        <div style="width:180px; min-width:180px; flex:0 0 180px;">
                            <select name="monthTo"
                                    class="form-select month-select"
                                    form="filterForm">
                            @for (int m = 1; m <= 12; m++)
                            {
                                <option value="@m" selected="@(monthTo == m)">
                                    @(new DateTime(year, m, 1).ToString("MMMM", new System.Globalization.CultureInfo("th-TH")))
                                </option>
                            }
                            </select>
                        </div>

                        <button type="submit"
                                form="filterForm"
                                class="btn btn-primary toolbar-btn">
                            แสดง
                        </button>

                    </form>
                    <div class="workload-toolbar">
                    </div>
                </div>

            </div>

            <div class="table-responsive workload-responsive-wrap">

                <table class="table table-bordered align-middle text-center workload-table">

                    <thead>
                        <tr>
                            <th class="sticky-employee-col" style="min-width:320px; width:320px;">
                                ผู้รับผิดชอบ
                            </th>

                            @for (int i = 0; i < totalDays; i++)
                            {
                                var date = firstDay.AddDays(i);
                                bool isToday = date.Date == DateTime.Today;
                                bool isWeekend = date.DayOfWeek == DayOfWeek.Saturday || date.DayOfWeek == DayOfWeek.Sunday;
                                bool isNewYear = date.Month == 1 && date.Day == 1;

                                <th class="@(isToday ? "today-column-header" : "") @(isWeekend ? "weekend-column" : "") @(isNewYear ? "newyear-column" : "")"
                                    style="min-width:40px; width:40px; position:relative;">

                                    <div style="font-size:9px;color:#64748b;line-height:1; margin-bottom:2px;">
                                        @(date.Year + 543)
                                    </div>

                                    <div style="font-size:10px;font-weight:700;color:#2563eb;line-height:1; margin-bottom:2px; white-space:nowrap;">
                                        @date.ToString("MMM", new System.Globalization.CultureInfo("th-TH"))
                                    </div>

                                    <div>@date.Day</div>

                                    <small class="text-muted" style="font-size:10px;">
                                        @date.ToString("ddd")
                                    </small>
                                </th>
                            }
                        </tr>
                    </thead>

                    <tbody>

                        @foreach (var emp in employees
                            .Where(e => string.IsNullOrEmpty(selectedEmpId)
                                || e.Key.EmpId.ToString() == selectedEmpId))
                        {
                            var empWorks = Model
                                .Where(x => x.EmpId == emp.Key.EmpId)
                                .OrderBy(x => x.Phase!.ProjectId)
                                .ThenBy(x => x.Phase!.PhaseSort)
                                .ToList();

                            var phaseRows = empWorks
                                .GroupBy(x => x.AssignId)
                                .Select(g => g.First())
                                .ToList();

                            foreach (var phase in phaseRows)
                            {
                                <tr>

                                    <td class="text-start bg-light phase-project-cell">
                                        @if (phase == phaseRows.First())
                                        {
                                            <div class="fw-bold mb-2">👤 @emp.Key.EmpName</div>
                                        }

                                        <div class="fw-bold" style="font-size:13px;color:#1f2937;line-height:1.2;">
                                            @phase.Phase?.Project?.ProjectName
                                        </div>

                                        <div style="font-size:12px;color:#6b7280;line-height:1.2;">
                                            🏷️ @phase.Role
                                        </div>

                                        <div style="font-size:11px;color:#6b7280;line-height:1.3; margin-top:4px;">
                                            📅 @(phase.PlanStart?.ToString("dd MMM yy", new System.Globalization.CultureInfo("th-TH")))
                                            -
                                            @(phase.PlanEnd?.ToString("dd MMM yy", new System.Globalization.CultureInfo("th-TH")))
                                        </div>
                                        @if (phase.Phase?.PeriodStartDate.HasValue == true || phase.Phase?.PeriodEndDate.HasValue == true)
                                        {
                                            <div style="font-size:11px;color:#7c3aed;line-height:1.3; margin-top:3px;">
                                                🗓️ งวดงาน:
                                                @(phase.Phase?.PeriodStartDate?.ToString("dd MMM yy", new System.Globalization.CultureInfo("th-TH")) ?? "-")
                                                -
                                                @(phase.Phase?.PeriodEndDate?.ToString("dd MMM yy", new System.Globalization.CultureInfo("th-TH")) ?? "-")
                                            </div>
                                        }
                                    </td>

                                    @for (int i = 0; i < totalDays; i++)
                                    {
                                        var currentDate = firstDay.AddDays(i);
                                        bool isToday = currentDate.Date == DateTime.Today;
                                        bool isWeekend = currentDate.DayOfWeek == DayOfWeek.Saturday || currentDate.DayOfWeek == DayOfWeek.Sunday;
                                        bool isNewYear = currentDate.Month == 1 && currentDate.Day == 1;

                                        bool isWorking = phase.PlanStart.HasValue
                                            && phase.PlanEnd.HasValue
                                            && currentDate >= phase.PlanStart.Value
                                            && currentDate <= phase.PlanEnd.Value;

                                        string bgColor = phase.WorkStatus == "DONE"
                                            ? "#A3E4B5"
                                            : "#FFE69C";

                                        <td class="timeline-cell p-0 align-middle @(isToday ? "today-column" : "") @(isWeekend ? "weekend-column" : "") @(isNewYear ? "newyear-column" : "")">
                                            @if (isWorking)
                                            {
                                                <a href="/PhaseAssigns?projectId=@phase.Phase?.ProjectId&phaseId=@phase.Phase?.PhaseId"
                                                   class="text-decoration-none d-block h-100">

                                                    <div class="work-item connected-work"
                                                         style="background:@bgColor">
                                                    </div>
                                                </a>
                                            }
                                        </td>
                                    }

                                </tr>
                            }
                        }

                    </tbody>

                </table>

            </div>

        </div>
    </div>

</div>

<style>
    .workload-table th {
        background: linear-gradient(180deg,#f8fafc,#eef2f7);
        vertical-align: middle;
        white-space: nowrap;
        position: sticky;
        top: 0;
        z-index: 10;
        font-size: 13px;
        font-weight: 700;
        border-bottom: 2px solid #dee2e6;
    }

    .sticky-employee-col {
        position: sticky;
        left: 0;
        z-index: 20 !important;
        background: linear-gradient(180deg,#f8fafc,#eef2f7) !important;
        box-shadow: 4px 0 8px rgba(0,0,0,.04);
    }

    .workload-table {
        min-width: max-content;
    }

    .timeline-cell {
        height: 60px;
        min-width: 34px;
        width: 34px;
        padding: 0 !important;
        vertical-align: middle !important;
        background: #fff;
        position: relative;
        border-left: 1px solid #edf1f5 !important;
        border-right: 1px solid #edf1f5 !important;
    }

    .workload-table tbody tr td {
        border-top: 1px solid #edf1f5 !important;
        border-bottom: 1px solid #edf1f5 !important;
    }

    .work-item {
        border-radius: 0;
        height: 30px;
        min-height: 30px;
        margin: 0 !important;
        transition: .15s ease;
        border: 0;
        position: relative;
        top: 50%;
        transform: translateY(-50%);
    }

    .work-item:hover {
        transform: translateY(calc(-50% - 1px));
        box-shadow: 0 4px 12px rgba(0,0,0,.14);
    }

    .connected-work {
        width: calc(100% + 2px);
        margin-left: -1px;
        border-radius: 0;
        position: relative;
        z-index: 2;
    }

    .phase-project-cell {
        min-width: 320px;
        width: 320px;
        max-width: 320px;
        vertical-align: middle;
        background: #fbfcfd !important;
        border-right: 2px solid #e9ecef;
        padding: 10px 14px !important;
        position: sticky;
        left: 0;
        z-index: 5;
        box-shadow: 4px 0 8px rgba(0,0,0,.04);
    }

    .weekend-column {
        background: #f8fafc !important;
    }
    .newyear-column {
        background: #ffe4e6 !important;
    }

    .today-column {
        background: #eef6ff !important;
        box-shadow: inset 2px 0 0 #3b82f6,
                    inset -2px 0 0 #3b82f6;
    }

    .today-column-header {
        background: linear-gradient(180deg,#dbeafe,#bfdbfe) !important;
        color: #1d4ed8 !important;
        box-shadow: inset 2px 0 0 #3b82f6,
                    inset -2px 0 0 #3b82f6;
    }

    .workload-legend {
        margin-top: -2px;
    }

    .legend-dot {
        width: 12px;
        height: 12px;
        border-radius: 999px;
        display: inline-block;
    }

    .legend-done {
        background: #A3E4B5;
        border: 1px solid #65b87d;
    }

    .legend-progress {
        background: #FFE69C;
        border: 1px solid #d4b85f;
    }
</style>


<style>
.employee-filter-wrap {
    display: inline-flex !important;
    align-items: center;
    gap: 8px !important;
    flex-wrap: wrap !important;
    flex: none !important;
    width: 100% !important;
    max-width: none !important;
    overflow: visible !important;
    white-space: nowrap;
}

.emp-select-fixed,
#empFilter {
    width: 100% !important;
    min-width: 100% !important;
    max-width: 100% !important;

    height: 34px !important;
    min-height: 34px !important;
    max-height: 34px !important;

    padding: 0 38px 0 10px !important;
    line-height: 34px !important;
    box-sizing: border-box !important;

    white-space: nowrap !important;
    overflow: hidden !important;
    text-overflow: ellipsis !important;
}

/* replaced month-select styles */
.month-select {
    width: 100% !important;
    min-width: 100% !important;
    max-width: 100% !important;

    height: 34px !important;
    min-height: 34px !important;
    max-height: 34px !important;

    padding: 0 38px 0 10px !important;
    line-height: 34px !important;
    box-sizing: border-box !important;

    white-space: nowrap !important;
    overflow: hidden !important;
    text-overflow: ellipsis !important;
}
</style>

<style>
.workload-toolbar {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: nowrap !important;
    white-space: nowrap;
    overflow-x: auto;
    overflow-y: hidden;
    min-width: 0;
    scrollbar-width: thin;
    scrollbar-color: #0f766e #e8eef6;
}

.workload-toolbar::-webkit-scrollbar {
    height: 6px;
}

.workload-toolbar::-webkit-scrollbar-thumb {
    background: #0f766e;
    border-radius: 999px;
}
</style>
<style>
/* ============================
   Responsive
============================ */
@@media (max-width: 1200px) {
    .phase-project-cell,
    .sticky-employee-col {
        min-width: 280px !important;
        width: 280px !important;
        max-width: 280px !important;
    }
}

@@media (max-width: 992px) {
    .workload-responsive-wrap {
        overflow-x: auto !important;
        overflow-y: hidden !important;
        -webkit-overflow-scrolling: touch;
    }

    .workload-table {
        min-width: max-content !important;
        width: max-content !important;
        table-layout: auto;
    }
    .filter-toolbar {
        overflow: visible !important;
        white-space: normal !important;
    }

    .employee-filter-wrap {
        display: flex !important;
        flex-wrap: wrap !important;
        gap: 10px !important;
        width: 100% !important;
        min-width: unset !important;
        white-space: normal !important;
    }

    .phase-project-cell,
    .sticky-employee-col {
        min-width: 220px !important;
        width: 220px !important;
        max-width: 220px !important;
    }

    .timeline-cell {
        min-width: 30px;
        width: 30px;
    }
}

@@media (max-width: 768px) {
    .workload-responsive-wrap {
        overflow-x: auto !important;
        overflow-y: hidden !important;
        -webkit-overflow-scrolling: touch;
    }

    .workload-table {
        min-width: max-content !important;
        width: max-content !important;
        table-layout: auto;
    }
    .card-body {
        padding: 12px !important;
    }

    .filter-toolbar {
        overflow: visible !important;
    }

    .employee-filter-wrap {
        display: flex !important;
        flex-wrap: wrap !important;
        gap: 10px !important;
        align-items: end;
        width: 100% !important;
    }

    .employee-filter-wrap > div {
        flex: 1 1 160px !important;
        min-width: 160px !important;
    }

    .employee-filter-wrap button {
        width: 100%;
        min-height: 38px;
        margin-top: 4px;
    }

    .employee-filter-wrap label,
    .employee-filter-wrap span {
        width: 100%;
        margin-bottom: -4px;
    }

    .phase-project-cell,
    .sticky-employee-col {
        min-width: 200px !important;
        width: 200px !important;
        max-width: 200px !important;
        padding: 8px 10px !important;
    }

    /* ลดความสูง header วันที่ */
    .workload-table th small {
        display: none;
    }

    .workload-table th div:first-child {
        display: none;
    }

    .timeline-cell {
        min-width: 20px;
        width: 20px;
        height: 48px;
    }

    .work-item {
        height: 24px;
        min-height: 24px;
    }

    .workload-table th {
        font-size: 11px;
    }

    .workload-table td {
        font-size: 12px;
    }

    .employee-filter-wrap label {
        font-size: 12px;
    }
}

.workload-page-wrap {
    width: 100% !important;
    max-width: 100vw !important;
}

@@media (max-width: 992px) {
    .container-fluid {
        padding-left: 8px !important;
        padding-right: 8px !important;
    }

    .card {
        width: 100% !important;
        max-width: 100% !important;
        overflow: visible !important;
    }
}

@@media (max-width: 768px) {
    .phase-project-cell,
    .sticky-employee-col {
        min-width: 120px !important;
        width: 120px !important;
        max-width: 120px !important;
        font-size: 11px !important;
        padding: 6px !important;
    }

    .timeline-cell {
        min-width: 16px !important;
        width: 16px !important;
    }
}
