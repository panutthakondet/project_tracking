@model ProjectTracking.Models.ProjectSupportOrder
@using Microsoft.AspNetCore.Http
@using System.Globalization
@using System.Linq

@{
    ViewData["Title"] = "รายละเอียดงาน Support Dev";

    var order = Model ?? new ProjectTracking.Models.ProjectSupportOrder();
    var th = new CultureInfo("th-TH");
    var beforeImages = order.Images?.ToList() ?? new List<ProjectTracking.Models.ProjectSupportImage>();
    var afterImages = order.FixImages?
        .Where(x => string.Equals(x.ImageType, "AFTER", StringComparison.OrdinalIgnoreCase))
        .ToList() ?? new List<ProjectTracking.Models.ProjectSupportFixImage>();
    var gitHistories = ViewBag.GitHistories as IEnumerable<ProjectTracking.Models.ProjectSupportOrderGitHistory>
        ?? new List<ProjectTracking.Models.ProjectSupportOrderGitHistory>();

    bool CanMenu(string key)
    {
        var role = (Context.Session.GetString("Role") ?? "").Trim();
        if (role.Equals("ADMIN", StringComparison.OrdinalIgnoreCase)) return true;

        var menus = Context.Session.GetString("Menus") ?? "";
        return menus.Split(',', StringSplitOptions.RemoveEmptyEntries)
            .Any(x => string.Equals(x.Trim(), key, StringComparison.OrdinalIgnoreCase));
    }

    string TextOrDash(string? value) => string.IsNullOrWhiteSpace(value) ? "-" : value.Trim();
    string DateText(DateTime? value) => value?.ToString("dd/MM/yyyy", th) ?? "-";

    string PriorityClass(string? priority) => (priority ?? "").Trim().ToUpperInvariant() switch
    {
        "URGENT" => "danger",
        "HIGH" => "danger",
        "MEDIUM" => "warning",
        "LOW" => "success",
        _ => "muted"
    };

    string StatusClass(string? status) => (status ?? "").Trim().ToUpperInvariant() switch
    {
        "OPEN" => "muted",
        "WIP" => "warning",
        "FIXED" => "success",
        "FAIL" => "danger",
        "PASS" => "success",
        "REJECT" => "muted",
        _ => "muted"
    };

    string DevStatusClass(string? status) => (status ?? "").Trim().ToUpperInvariant() switch
    {
        "WIP" => "warning",
        "FIXED" => "success",
        _ => "muted"
    };
}

<style>
    .support-dev-details-page {
        width: 100%;
        max-width: 100%;
        margin: 0;
    }

    .support-dev-hero {
        color: #fff;
        background: linear-gradient(135deg, #10233f, #1f3b63);
        border-radius: 20px;
        padding: 24px;
        margin-bottom: 18px;
        box-shadow: 0 18px 40px rgba(15, 35, 64, .22);
    }

    .support-dev-hero-top {
        display: flex;
        align-items: flex-start;
        gap: 20px;
    }

    .support-dev-hero-main {
        min-width: 0;
        flex: 1 1 auto;
    }

    .support-dev-eyebrow {
        color: rgba(216, 230, 247, .86);
        font-size: 12px;
        font-weight: 900 !important;
        letter-spacing: .08em;
        text-transform: uppercase;
        margin-bottom: 4px;
    }

    .support-dev-title {
        margin: 0;
        color: #fff;
        font-size: clamp(24px, 3vw, 34px);
        font-weight: 900 !important;
        line-height: 1.22;
        overflow-wrap: anywhere;
    }

    .support-dev-subtitle {
        max-width: 920px;
        margin: 8px 0 0;
        color: #d8e6f7;
        font-weight: 700;
        line-height: 1.5;
    }

    .support-dev-actions {
        display: flex;
        flex-wrap: wrap;
        justify-content: flex-end;
        align-items: center;
        gap: 10px;
        width: 100%;
        margin-top: 14px;
    }

    .support-dev-hero .support-dev-actions {
        flex-wrap: wrap !important;
        justify-content: flex-end !important;
        gap: 12px !important;
    }

    .support-dev-actions .btn {
        flex: 0 0 auto !important;
        width: auto !important;
        min-width: 174px !important;
        max-width: 100% !important;
        min-height: 42px;
        border-radius: 999px !important;
        font-size: .95rem;
        font-weight: 900;
        line-height: 1.25;
        padding: .58rem 1.55rem !important;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        white-space: nowrap;
        box-shadow: 0 8px 18px rgba(20, 184, 166, .14);
    }

    .support-dev-hero .support-dev-actions .btn-outline-light {
        min-width: 190px !important;
    }

    .support-dev-hero .support-dev-actions .btn-primary {
        min-width: 174px !important;
    }

    .support-dev-hero .btn-primary {
        color: #062b2f !important;
        background: linear-gradient(135deg, #8df1e4, #23c7c8) !important;
        border-color: transparent !important;
    }

    .support-dev-hero .btn-outline-light {
        color: #fff !important;
        background: rgba(255,255,255,.08);
        border-color: rgba(255,255,255,.55) !important;
    }

    .support-dev-hero .btn-outline-light:hover {
        background: rgba(255,255,255,.18) !important;
    }

    .support-dev-summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
        gap: 12px;
        margin-bottom: 18px;
    }

    .support-dev-summary-card,
    .support-dev-panel {
        background: #fff;
        border: 1px solid #d8e3ef;
        border-radius: 18px;
        box-shadow: 0 10px 24px rgba(15, 35, 64, .08);
    }

    .support-dev-summary-card {
        min-height: 90px;
        padding: 14px 16px;
    }

    .support-dev-summary-card small {
        display: block;
        margin-bottom: 6px;
        color: #000;
        font-size: 12px;
        font-weight: 900 !important;
        text-transform: uppercase;
    }

    .support-dev-summary-card b {
        color: #000;
        font-size: 16px;
        font-weight: 400;
        line-height: 1.35;
        word-break: break-word;
    }

    .support-dev-content-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
        gap: 16px;
        margin-bottom: 18px;
    }

    .support-dev-bottom-stack {
        display: grid;
        gap: 16px;
        margin-bottom: 18px;
    }

    .support-dev-panel {
        padding: 18px;
    }

    .support-dev-panel h3 {
        margin: 0 0 12px;
        color: #000;
        font-size: 18px;
        font-weight: 900;
    }

    .support-dev-info-block {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 10px 18px;
        padding: 14px;
        border: 1px solid #e2e8f0;
        border-radius: 14px;
        background: #f8fafc;
    }

    .support-dev-info-line {
        min-width: 0;
        color: #000;
        font-size: 16px;
        font-weight: 400;
        line-height: 1.45;
        overflow-wrap: anywhere;
    }

    .support-dev-info-line .support-dev-info-label {
        display: inline-block;
        margin-right: 6px;
        color: #000;
        font-size: inherit;
        font-weight: 700;
        line-height: inherit;
        text-transform: uppercase;
    }

    .support-dev-info-value {
        color: #000;
        font-size: inherit;
        font-weight: 400 !important;
        line-height: inherit;
    }

    .support-dev-text-box {
        min-height: 150px;
        color: #000;
        font-weight: 400;
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 14px;
        padding: 14px;
        line-height: 1.65;
        white-space: pre-wrap;
        word-break: break-word;
    }

    .support-dev-gallery-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 12px;
    }

    .support-dev-count {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 30px;
        padding: 5px 12px;
        border-radius: 999px;
        color: #0f766e;
        background: rgba(20, 184, 166, .14);
        font-size: .84rem;
        font-weight: 900;
        white-space: nowrap;
    }

    .support-dev-gallery-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
        gap: 12px;
    }

    .support-dev-image-card {
        display: block;
        width: 100%;
        border: 1px solid #d8e3ef;
        border-radius: 14px;
        padding: 8px;
        background: #f8fafc;
        overflow: hidden;
        cursor: pointer;
        transition: transform .15s ease, box-shadow .15s ease;
    }

    .support-dev-image-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 24px rgba(15, 35, 64, .14);
    }

    .support-dev-image-card img {
        display: block;
        width: 100%;
        aspect-ratio: 1 / 1;
        object-fit: cover;
        border-radius: 10px;
    }

    .support-dev-empty {
        border: 1px dashed #cbd5e1;
        border-radius: 14px;
        color: #000;
        background: #f8fafc;
        padding: 18px;
        text-align: center;
        font-weight: 400;
    }

    .support-dev-git-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 8px;
    }

    .support-dev-git-table th {
        padding: 0 10px;
        color: #475569;
        font-size: .9rem;
        font-weight: 700;
    }

    .support-dev-git-table td {
        padding: 10px;
        color: #000;
        background: #f8fafc;
        border-top: 1px solid #e2e8f0;
        border-bottom: 1px solid #e2e8f0;
        font-weight: 400;
    }

    .support-dev-git-table td:first-child {
        border-left: 1px solid #e2e8f0;
        border-radius: 12px 0 0 12px;
    }

    .support-dev-git-table td:last-child {
        border-right: 1px solid #e2e8f0;
        border-radius: 0 12px 12px 0;
    }

    .support-dev-git-badge {
        display: inline-flex;
        align-items: center;
        min-height: 28px;
        padding: 4px 10px;
        border-radius: 999px;
        color: #075985;
        background: #e0f2fe;
        font-size: .86rem;
        font-weight: 700;
    }

    .support-dev-git-id {
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
        overflow-wrap: anywhere;
    }

    .status-text {
        color: #000;
        font-weight: 400 !important;
        font-size: inherit !important;
        white-space: nowrap;
    }

    .status-text.success,
    .status-text.warning,
    .status-text.info,
    .status-text.danger,
    .status-text.muted { color: #000; }

    .support-dev-details-page :is(
        .support-dev-summary-card,
        .support-dev-summary-label,
        .support-dev-summary-value,
        .support-dev-info-line,
        .support-dev-info-label,
        .support-dev-info-value,
        .support-dev-text-box,
        .support-dev-count,
        .support-dev-empty
    ) {
        font-size: 1rem !important;
        line-height: 1.45 !important;
        letter-spacing: 0 !important;
    }

    .support-dev-details-page :is(
        .support-dev-summary-label,
        .support-dev-info-label,
        .support-dev-panel h3,
        .support-dev-gallery-head h3
    ) {
        color: #000 !important;
        font-size: 1rem !important;
        font-weight: 700 !important;
        line-height: 1.45 !important;
        letter-spacing: 0 !important;
        text-transform: none !important;
    }

    .support-dev-summary-label {
        display: block;
        margin-bottom: 6px;
    }

    .support-dev-details-page :is(
        .support-dev-summary-value,
        .support-dev-info-value,
        .support-dev-text-box,
        .status-text
    ) {
        color: #000 !important;
        font-size: 1rem !important;
        font-weight: 400 !important;
        line-height: 1.45 !important;
        letter-spacing: 0 !important;
    }

    @@media (max-width: 991.98px) {
        .support-dev-hero-top,
        .support-dev-content-grid {
            grid-template-columns: 1fr;
            display: grid;
        }

        .support-dev-actions {
            justify-content: flex-start;
        }

        .support-dev-summary-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .support-dev-info-block {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 575.98px) {
        .support-dev-summary-grid {
            grid-template-columns: 1fr;
        }

        .support-dev-actions .btn {
            flex: 1 1 100% !important;
            min-width: 0 !important;
            width: 100% !important;
            white-space: normal;
        }
    }
</style>

<div class="support-dev-details-page">
    <section class="support-dev-hero">
        <div class="support-dev-hero-top">
            <div class="support-dev-hero-main">
                <div class="support-dev-eyebrow">Support Dev #@order.OrderId</div>
                <h1 class="support-dev-title">รายละเอียดการแก้ไข Support</h1>
                <p class="support-dev-subtitle">ตรวจรายละเอียดงาน Support รูปประกอบ และสถานะการแก้ไขของทีมพัฒนา</p>
            </div>
        </div>
        <div class="support-dev-actions">
            <a asp-action="Index"
               asp-route-projectId="@order.ProjectId"
               class="btn btn-outline-light">
                ไปหน้า Support
            </a>
            @if (CanMenu("SupportOrdersDev.Edit"))
            {
                <a asp-action="Edit"
                   asp-route-id="@order.OrderId"
                   class="btn btn-primary">
                    ไปหน้าแก้ไข
                </a>
            }
        </div>
    </section>

    <section class="support-dev-summary-grid">
        <div class="support-dev-summary-card">
            <span class="support-dev-summary-label">Priority</span>
            <span class="support-dev-summary-value status-text @PriorityClass(order.Priority)">@TextOrDash(order.Priority)</span>
        </div>
        <div class="support-dev-summary-card">
            <span class="support-dev-summary-label">Status</span>
            <span class="support-dev-summary-value status-text @StatusClass(order.Status)">@TextOrDash(order.Status)</span>
        </div>
        <div class="support-dev-summary-card">
            <span class="support-dev-summary-label">Dev Status</span>
            <span class="support-dev-summary-value status-text @DevStatusClass(order.DevStatus)">@TextOrDash(order.DevStatus)</span>
        </div>
        <div class="support-dev-summary-card">
            <span class="support-dev-summary-label">Images</span>
            <span class="support-dev-summary-value">@beforeImages.Count ก่อนแก้ / @afterImages.Count หลังแก้</span>
        </div>
    </section>

    <section class="support-dev-bottom-stack">
        <article class="support-dev-panel">
            <h3>ข้อมูล Support</h3>
            <div class="support-dev-info-block">
                <div class="support-dev-info-line"><span class="support-dev-info-label">Project:</span><span class="support-dev-info-value">@TextOrDash(order.Project?.ProjectDisplayName)</span></div>
                <div class="support-dev-info-line"><span class="support-dev-info-label">Title:</span><span class="support-dev-info-value">@TextOrDash(order.OrderTitle)</span></div>
                <div class="support-dev-info-line">
                    <span class="support-dev-info-label">Assign To:</span><span class="support-dev-info-value">@(order.Employee != null ? $"{order.Employee.EmpName} ({order.Employee.Position})" : "-")</span>
                </div>
                <div class="support-dev-info-line"><span class="support-dev-info-label">วันที่เริ่ม:</span><span class="support-dev-info-value">@DateText(order.StartDate)</span></div>
                <div class="support-dev-info-line"><span class="support-dev-info-label">วันที่สิ้นสุด:</span><span class="support-dev-info-value">@DateText(order.EndDate)</span></div>
            </div>
        </article>

        <article class="support-dev-panel">
            <h3>รายละเอียดงาน</h3>
            <div class="support-dev-text-box">@TextOrDash(order.OrderDetail)</div>
        </article>

        <article class="support-dev-panel">
            <h3>รายละเอียดการแก้ไข</h3>
            <div class="support-dev-text-box">@TextOrDash(order.DevDetail)</div>
        </article>

        <article class="support-dev-panel">
            <h3>ประวัติ Git</h3>
            @if (gitHistories.Any())
            {
                <table class="support-dev-git-table">
                    <thead>
                        <tr>
                            <th>ประเภท</th>
                            <th>Git ID</th>
                            <th>วันที่บันทึก</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var row in gitHistories)
                        {
                            <tr>
                                <td><span class="support-dev-git-badge">@row.GitType</span></td>
                                <td class="support-dev-git-id">@row.GitId</td>
                                <td>@row.EntryDate.ToString("dd/MM/yyyy HH:mm")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="support-dev-empty">ยังไม่มีประวัติ Git</div>
            }
        </article>
    </section>

    <section class="support-dev-content-grid">
        <article class="support-dev-panel">
            <div class="support-dev-gallery-head">
                <h3>รูปจาก BA</h3>
                <span class="support-dev-count">@beforeImages.Count รูป</span>
            </div>

            @if (beforeImages.Count > 0)
            {
                <div class="support-dev-gallery-grid">
                    @foreach (var img in beforeImages)
                    {
                        var src = string.IsNullOrWhiteSpace(img.FilePath) ? "" : Url.Content("~/" + img.FilePath.TrimStart('/'));
                        <button type="button"
                                class="support-dev-image-card"
                                onclick="previewSupportDevImage('@src')"
                                aria-label="ดูรูปจาก BA">
                            <img src="@src" alt="@TextOrDash(img.FileName)" />
                        </button>
                    }
                </div>
            }
            else
            {
                <div class="support-dev-empty">ไม่มีรูปจาก BA</div>
            }
        </article>

        <article class="support-dev-panel">
            <div class="support-dev-gallery-head">
                <h3>รูปหลังแก้</h3>
                <span class="support-dev-count">@afterImages.Count รูป</span>
            </div>

            @if (afterImages.Count > 0)
            {
                <div class="support-dev-gallery-grid">
                    @foreach (var img in afterImages)
                    {
                        var src = string.IsNullOrWhiteSpace(img.FilePath) ? "" : Url.Content("~/" + img.FilePath.TrimStart('/'));
                        <button type="button"
                                class="support-dev-image-card"
                                onclick="previewSupportDevImage('@src')"
                                aria-label="ดูรูปหลังแก้">
                            <img src="@src" alt="Support image after fix" />
                        </button>
                    }
                </div>
            }
            else
            {
                <div class="support-dev-empty">ไม่มีรูปหลังแก้</div>
            }
        </article>
    </section>
</div>

<div class="modal fade" id="supportDevImagePreviewModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content bg-dark">
            <div class="modal-body text-center p-2">
                <img id="supportDevPreviewImage" src="" class="img-fluid rounded" alt="preview" />
            </div>
        </div>
    </div>
</div>

<script>
function prepareSupportDevImagePreviewModal(modalId) {
    const modalEl = document.getElementById(modalId);
    if (!modalEl) return null;

    if (modalEl.parentElement !== document.body) {
        document.body.appendChild(modalEl);
    }

    document.querySelectorAll('.modal-backdrop').forEach(backdrop => backdrop.remove());
    document.body.classList.remove('modal-open');
    document.body.style.removeProperty('overflow');
    document.body.style.removeProperty('padding-right');

    modalEl.style.zIndex = '26010';
    return modalEl;
}

function previewSupportDevImage(src) {
    const img = document.getElementById("supportDevPreviewImage");
    img.src = src;

    const modalEl = prepareSupportDevImagePreviewModal("supportDevImagePreviewModal");
    if (!modalEl) return;

    const modal = bootstrap.Modal.getOrCreateInstance(modalEl, {
        backdrop: true,
        keyboard: true
    });
    modal.show();

    window.setTimeout(function () {
        modalEl.style.zIndex = '26010';
        document.querySelectorAll('.modal-backdrop').forEach(function (backdrop) {
            backdrop.style.zIndex = '26000';
        });
    }, 0);
}
</script>
